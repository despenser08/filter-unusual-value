import path from "path";

export const botId = "";
export const normalLowestValue = 0;
export const differenceFilter = 0;
export const dataFile = path.join(__dirname, "..", `result-${botId}.json`);
export const savePath = path.join(__dirname, "..", "out");
export const saveFiltered = true;
export const saveCode = true;
export const showFiltered = false;
